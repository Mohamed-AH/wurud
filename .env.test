# Test Environment Configuration
# This file is used for automated testing

NODE_ENV=test
PORT=3001

# MongoDB will be handled by mongodb-memory-server (in-memory database)
# MONGODB_URI will be set dynamically in tests

# Test session secret
SESSION_SECRET=test-session-secret-for-automated-testing

# Google OAuth (not needed for most tests)
GOOGLE_CLIENT_ID=test-client-id
GOOGLE_CLIENT_SECRET=test-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# Admin emails for testing
ADMIN_EMAILS=admin@test.com,admin2@test.com

# File upload (use test directory)
UPLOAD_DIR=./tests/uploads
MAX_FILE_SIZE=62914560

# Site URL
SITE_URL=http://localhost:3001
