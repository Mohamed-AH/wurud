<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Duroos</title>
  <!-- Load marked library for markdown preview -->
  <script src="https://cdn.jsdelivr.net/npm/marked@11.1.1/marked.min.js"></script>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f5f5;
      color: #333;
    }
    .header {
      background: #1A5F5A;
      color: white;
      padding: 16px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header h1 { font-size: 24px; font-weight: 600; }
    .header-nav { display: flex; align-items: center; gap: 16px; }
    .header-nav a { color: white; text-decoration: none; opacity: 0.9; }
    .header-nav a:hover { opacity: 1; }
    .container { max-width: 1000px; margin: 0 auto; padding: 32px 24px; }
    .card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      padding: 32px;
    }
    .form-group {
      margin-bottom: 24px;
    }
    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      color: #2C3E35;
    }
    .label-help {
      font-size: 12px;
      color: #6c757d;
      font-weight: 400;
      margin-right: 8px;
    }
    input, textarea, select {
      width: 100%;
      padding: 12px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 14px;
      font-family: inherit;
    }
    textarea {
      min-height: 120px;
      resize: vertical;
      font-family: 'Courier New', monospace;
      direction: rtl;
    }
    .markdown-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }
    .markdown-editor, .markdown-preview {
      border: 1px solid #ddd;
      border-radius: 6px;
      padding: 12px;
      min-height: 200px;
    }
    .markdown-editor textarea {
      border: none;
      padding: 0;
      width: 100%;
      min-height: 200px;
    }
    .markdown-preview {
      background: #f8f9fa;
      overflow-y: auto;
    }
    .markdown-label {
      font-size: 12px;
      font-weight: 600;
      color: #6c757d;
      margin-bottom: 8px;
      text-transform: uppercase;
    }
    .markdown-help {
      font-size: 12px;
      color: #6c757d;
      margin-top: 8px;
      padding: 8px;
      background: #f8f9fa;
      border-radius: 4px;
    }
    .markdown-help code {
      background: white;
      padding: 2px 6px;
      border-radius: 3px;
      font-family: 'Courier New', monospace;
    }
    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      margin-left: 8px;
    }
    .btn-primary {
      background: #1A5F5A;
      color: white;
    }
    .btn-primary:hover {
      background: #145047;
    }
    .btn-secondary {
      background: #6c757d;
      color: white;
    }
    .btn-secondary:hover {
      background: #5a6268;
    }
    .actions {
      display: flex;
      gap: 12px;
      margin-top: 32px;
    }
    .required {
      color: #dc3545;
      margin-right: 4px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= isEdit ? '✏️ تعديل الشيخ' : '➕ إضافة شيخ جديد' %></h1>
    <div class="header-nav">
      <a href="/admin/dashboard">لوحة التحكم</a>
      <a href="/admin/sheikhs">إدارة الشيوخ</a>
      <a href="/auth/logout">خروج</a>
    </div>
  </div>

  <div class="container">
    <div class="card">
      <form method="POST" action="<%= isEdit ? `/admin/sheikhs/${sheikh._id}/edit` : '/admin/sheikhs' %>">
        <div class="form-group">
          <label for="nameArabic">
            الاسم بالعربية
            <span class="required">*</span>
          </label>
          <input
            type="text"
            id="nameArabic"
            name="nameArabic"
            value="<%= sheikh ? sheikh.nameArabic : '' %>"
            required
            placeholder="مثال: محمد بن عبدالوهاب"
          >
        </div>

        <div class="form-group">
          <label for="nameEnglish">الاسم بالإنجليزية</label>
          <input
            type="text"
            id="nameEnglish"
            name="nameEnglish"
            value="<%= sheikh ? (sheikh.nameEnglish || '') : '' %>"
            placeholder="Example: Muhammad ibn Abd al-Wahhab"
          >
        </div>

        <div class="form-group">
          <label for="honorific">
            اللقب التكريمي
            <span class="label-help">(الافتراضي: حفظه الله)</span>
          </label>
          <input
            type="text"
            id="honorific"
            name="honorific"
            value="<%= sheikh ? (sheikh.honorific || 'حفظه الله') : 'حفظه الله' %>"
            placeholder="حفظه الله / رحمه الله"
          >
        </div>

        <div class="form-group">
          <label for="photoUrl">
            رابط الصورة
            <span class="label-help">(اختياري)</span>
          </label>
          <input
            type="url"
            id="photoUrl"
            name="photoUrl"
            value="<%= sheikh ? (sheikh.photoUrl || '') : '' %>"
            placeholder="https://example.com/image.jpg"
          >
        </div>

        <div class="form-group">
          <label>
            نبذة عن الشيخ (عربي)
            <span class="label-help">- يدعم تنسيق Markdown</span>
          </label>
          <div class="markdown-container">
            <div>
              <div class="markdown-label">✍️ محرر</div>
              <div class="markdown-editor">
                <textarea
                  id="bioArabic"
                  name="bioArabic"
                  oninput="updatePreview('bioArabic', 'bioArabicPreview')"
                  placeholder="اكتب نبذة عن الشيخ هنا...

يمكنك استخدام:
- **نص عريض**
- *نص مائل*
- ### عناوين
- قوائم مرقمة أو نقطية"
                ><%= sheikh ? (sheikh.bioArabic || '') : '' %></textarea>
              </div>
            </div>
            <div>
              <div class="markdown-label">👁️ معاينة</div>
              <div class="markdown-preview" id="bioArabicPreview" dir="rtl"></div>
            </div>
          </div>
          <div class="markdown-help">
            💡 نصائح Markdown:
            <code>**عريض**</code>
            <code>*مائل*</code>
            <code>### عنوان</code>
            <code>- قائمة</code>
            <code>[رابط](url)</code>
          </div>
        </div>

        <div class="form-group">
          <label>
            نبذة عن الشيخ (إنجليزي)
            <span class="label-help">- يدعم تنسيق Markdown</span>
          </label>
          <div class="markdown-container">
            <div>
              <div class="markdown-label">✍️ Editor</div>
              <div class="markdown-editor">
                <textarea
                  id="bioEnglish"
                  name="bioEnglish"
                  oninput="updatePreview('bioEnglish', 'bioEnglishPreview')"
                  placeholder="Write a brief biography...

You can use:
- **bold text**
- *italic text*
- ### headings
- numbered or bulleted lists"
                  dir="ltr"
                ><%= sheikh ? (sheikh.bioEnglish || '') : '' %></textarea>
              </div>
            </div>
            <div>
              <div class="markdown-label">👁️ Preview</div>
              <div class="markdown-preview" id="bioEnglishPreview" dir="ltr"></div>
            </div>
          </div>
          <div class="markdown-help">
            💡 Markdown tips:
            <code>**bold**</code>
            <code>*italic*</code>
            <code>### heading</code>
            <code>- list</code>
            <code>[link](url)</code>
          </div>
        </div>

        <div class="actions">
          <button type="submit" class="btn btn-primary">
            <%= isEdit ? '💾 حفظ التغييرات' : '➕ إضافة الشيخ' %>
          </button>
          <a href="/admin/sheikhs" class="btn btn-secondary">↩️ إلغاء</a>
        </div>
      </form>
    </div>
  </div>

  <script>
    // Initialize previews on page load
    window.addEventListener('DOMContentLoaded', () => {
      updatePreview('bioArabic', 'bioArabicPreview');
      updatePreview('bioEnglish', 'bioEnglishPreview');
    });

    function updatePreview(textareaId, previewId) {
      const textarea = document.getElementById(textareaId);
      const preview = document.getElementById(previewId);

      if (textarea && preview) {
        const markdown = textarea.value;
        const html = marked.parse(markdown);
        preview.innerHTML = html || '<em style="color: #999;">المعاينة ستظهر هنا...</em>';
      }
    }
  </script>
</body>
</html>
