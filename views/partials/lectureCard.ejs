<div class="lecture-card">
  <div class="lecture-icon">
    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"></path>
      <path d="M19 10v2a7 7 0 0 1-14 0v-2"></path>
      <line x1="12" y1="19" x2="12" y2="23"></line>
      <line x1="8" y1="23" x2="16" y2="23"></line>
    </svg>
  </div>

  <div class="lecture-content">
    <h3 class="lecture-title">
      <a href="/lectures/<%= lecture.slug ? encodeURIComponent(lecture.slug) : lecture._id %>"><%= lecture.titleArabic %></a>
    </h3>

    <div class="lecture-meta">
      <span class="lecture-sheikh">
        <% if (lecture.sheikhId) { %>
          <%= lecture.sheikhId.nameArabic %>
          <% if (lecture.sheikhId.honorific) { %>
            <%= lecture.sheikhId.honorific %>
          <% } %>
        <% } %>
      </span>

      <% if (lecture.seriesId) { %>
        <span class="lecture-divider">•</span>
        <span class="lecture-series"><%= lecture.seriesId.titleArabic %></span>
      <% } %>
    </div>

    <div class="lecture-info">
      <% if (lecture.duration && lecture.duration > 0) { %>
        <span class="lecture-duration">⏱️ <%= lecture.durationFormatted %></span>
        <span class="lecture-divider">•</span>
      <% } %>
      <span class="lecture-plays">▶️ <%= lecture.playCount %> مرة</span>
      <% if (lecture.dateRecordedHijri) { %>
        <span class="lecture-divider">•</span>
        <span class="lecture-date" title="تاريخ التسجيل (هجري)">
          📅 <%= formatHijriDate(lecture.dateRecordedHijri) %>
        </span>
      <% } %>
      <% if (lecture.category && lecture.category !== 'Other') { %>
        <span class="lecture-divider">•</span>
        <span class="lecture-category"><%= translateCategory(lecture.category) %></span>
      <% } %>
    </div>
  </div>

  <div class="lecture-actions">
    <% if (lecture.audioFileName) { %>
      <button class="btn-play" onclick="playAudio('<%= lecture._id %>', '<%= (lecture.titleArabic || '').replace(/'/g, "\\'") %>', '<%= lecture.sheikhId ? lecture.sheikhId.nameArabic.replace(/'/g, "\\'") : "" %>')" title="استماع">
        ▶️
      </button>
    <% } else { %>
      <button class="btn-play" disabled style="opacity: 0.5; cursor: not-allowed;" title="لا يوجد ملف صوتي">
        ▶️
      </button>
    <% } %>
    <a href="/download/<%= lecture._id %>" class="btn-download" title="تحميل">⬇️</a>
  </div>
</div>

<style>
  .lecture-card {
    background: white;
    border-radius: 12px;
    padding: 20px;
    display: flex;
    gap: 16px;
    align-items: center;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
  }

  .lecture-card:hover {
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
  }

  .lecture-icon {
    width: 48px;
    height: 48px;
    border-radius: 10px;
    background: linear-gradient(135deg, var(--accent-gold) 0%, var(--accent-amber) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--primary-dark);
  }

  .lecture-content {
    flex: 1;
    min-width: 0;
  }

  .lecture-title {
    font-size: 18px;
    margin-bottom: 8px;
    font-weight: 600;
  }

  .lecture-title a {
    color: var(--text-primary);
    text-decoration: none;
    transition: color 0.3s;
  }

  .lecture-title a:hover {
    color: var(--accent-gold);
  }

  .lecture-meta {
    color: var(--text-secondary);
    font-size: 14px;
    margin-bottom: 8px;
  }

  .lecture-sheikh {
    font-weight: 500;
    color: var(--accent-gold);
  }

  .lecture-info {
    color: var(--text-secondary);
    font-size: 13px;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    align-items: center;
  }

  .lecture-divider {
    opacity: 0.5;
  }

  .lecture-category {
    background: #f0f0f0;
    padding: 2px 8px;
    border-radius: 4px;
    font-size: 12px;
  }

  .lecture-actions {
    display: flex;
    gap: 8px;
    flex-shrink: 0;
  }

  .btn-play,
  .btn-download {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    text-decoration: none;
    font-size: 16px;
    transition: all 0.3s;
    border: 2px solid var(--accent-gold);
    cursor: pointer;
  }

  .btn-play {
    background: var(--accent-gold);
    color: var(--primary-dark);
  }

  .btn-play:hover {
    background: var(--accent-amber);
    transform: scale(1.05);
  }

  .btn-download {
    background: transparent;
    color: var(--accent-gold);
  }

  .btn-download:hover {
    background: var(--accent-gold);
    color: var(--primary-dark);
  }

  @media (max-width: 640px) {
    .lecture-card {
      flex-direction: column;
      align-items: flex-start;
    }

    .lecture-actions {
      width: 100%;
      justify-content: center;
    }

    .lecture-title {
      font-size: 16px;
    }
  }
</style>
