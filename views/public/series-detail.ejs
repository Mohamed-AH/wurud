<style>
  /* Brown/Gold Design for Series Detail Page */
  .series-detail-page {
    padding: 0;
  }

  .breadcrumb {
    background: var(--cream);
    padding: 16px 0;
    border-bottom: 2px solid var(--accent-amber);
  }

  .breadcrumb .container {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
  }

  .breadcrumb a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.3s;
  }

  .breadcrumb a:hover {
    color: var(--accent-gold);
  }

  .breadcrumb .separator {
    color: var(--text-muted);
  }

  .breadcrumb .current {
    color: var(--text-primary);
    font-weight: 600;
  }

  .series-hero {
    background: linear-gradient(135deg, var(--primary-brown) 0%, var(--sage) 100%);
    padding: 48px 0;
    border-bottom: 3px solid var(--accent-gold);
  }

  .series-category-badge {
    background: var(--accent-gold);
    color: var(--primary-dark);
    padding: 6px 16px;
    border-radius: 16px;
    font-size: 14px;
    font-weight: 600;
    display: inline-block;
    margin-bottom: 16px;
  }

  .series-title {
    font-family: var(--font-arabic-display);
    font-size: 42px;
    color: var(--cream);
    margin-bottom: 12px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
  }

  .series-title-en {
    font-size: 24px;
    color: var(--cream);
    opacity: 0.9;
    margin-bottom: 24px;
    font-weight: 400;
  }

  .series-sheikh {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--cream);
    font-size: 18px;
  }

  .series-sheikh a {
    color: var(--accent-gold);
    text-decoration: none;
    transition: color 0.3s;
  }

  .series-sheikh a:hover {
    color: var(--accent-amber);
  }

  .stats-section {
    padding: 40px 0;
    background: var(--paper);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 24px;
  }

  .stat-card {
    background: var(--cream);
    border-radius: 12px;
    padding: 24px;
    display: flex;
    align-items: center;
    gap: 16px;
    border: 2px solid var(--accent-amber);
    box-shadow: 0 4px 12px var(--shadow-warm);
  }

  .stat-icon {
    width: 56px;
    height: 56px;
    border-radius: 12px;
    background: linear-gradient(135deg, var(--accent-gold) 0%, var(--accent-amber) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .stat-icon svg {
    color: var(--primary-dark);
  }

  .stat-value {
    font-family: var(--font-arabic-display);
    font-size: 32px;
    color: var(--primary-dark);
    font-weight: 700;
  }

  .stat-label {
    color: var(--text-secondary);
    font-size: 14px;
  }

  .content-section {
    padding: 48px 0;
  }

  .section-title {
    font-family: var(--font-arabic-display);
    font-size: 28px;
    color: var(--primary-dark);
    margin-bottom: 24px;
    padding-bottom: 12px;
    border-bottom: 3px solid var(--accent-gold);
  }

  .description-card, .bio-card {
    background: var(--cream);
    border-radius: 12px;
    padding: 32px;
    border: 2px solid var(--accent-amber);
    box-shadow: 0 4px 12px var(--shadow-warm);
    margin-bottom: 48px;
  }

  .description-text, .bio-text {
    color: var(--text-secondary);
    line-height: 1.9;
    font-size: 18px;
  }

  .description-text-en, .bio-text-en {
    color: var(--text-muted);
    line-height: 1.7;
    font-size: 16px;
    margin-top: 16px;
    font-style: italic;
  }

  .btn-link {
    color: var(--accent-gold);
    text-decoration: none;
    font-weight: 600;
    margin-top: 16px;
    display: inline-block;
  }

  .btn-link:hover {
    color: var(--accent-amber);
  }

  .lectures-list {
    display: grid;
    gap: 16px;
  }

  .lecture-item {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    padding: 12px;
    border-radius: 12px;
    transition: all 0.3s ease;
  }

  .lecture-item:hover {
    background: rgba(193, 154, 107, 0.08);
  }

  .lecture-number-badge {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--accent-gold);
    color: var(--primary-dark);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-arabic-display);
    font-size: 20px;
    font-weight: 700;
    flex-shrink: 0;
    box-shadow: 0 2px 8px var(--shadow-warm);
  }

  .lecture-number-badge.unnumbered {
    background: transparent;
    border: 2px dashed var(--accent-amber);
    color: var(--text-muted);
  }

  .lecture-item-content {
    flex: 1;
  }

  .series-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 24px;
    margin-bottom: 48px;
  }

  .series-card {
    background: var(--cream);
    border-radius: 12px;
    padding: 24px;
    text-decoration: none;
    transition: all 0.3s;
    border: 2px solid var(--accent-amber);
    display: flex;
    gap: 16px;
    box-shadow: 0 4px 12px var(--shadow-warm);
  }

  .series-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 20px var(--shadow-warm);
    border-color: var(--accent-gold);
  }

  .series-icon {
    width: 48px;
    height: 48px;
    border-radius: 8px;
    background: var(--accent-gold);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .series-icon svg {
    color: var(--primary-dark);
  }

  .series-content {
    flex: 1;
  }

  .series-title {
    font-family: var(--font-arabic-display);
    font-size: 20px;
    color: var(--primary-dark);
    margin-bottom: 4px;
  }

  .series-title-en {
    color: var(--text-muted);
    font-size: 14px;
    margin-bottom: 12px;
  }

  .series-meta {
    display: flex;
    gap: 12px;
    font-size: 14px;
    color: var(--text-secondary);
  }

  .series-category {
    background: var(--accent-gold);
    color: var(--primary-dark);
    padding: 2px 8px;
    border-radius: 8px;
    font-weight: 600;
  }

  .series-count {
    color: var(--text-secondary);
  }

  .empty-state {
    text-align: center;
    padding: 80px 20px;
    color: var(--text-secondary);
  }

  .empty-icon {
    font-size: 64px;
    margin-bottom: 16px;
    opacity: 0.5;
  }

  /* Sort Controls */
  .sort-controls {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 16px 20px;
    background: var(--cream);
    border-radius: 12px;
    margin-bottom: 24px;
    border: 2px solid var(--accent-amber);
    flex-wrap: wrap;
  }

  .sort-label {
    font-size: 14px;
    font-weight: 600;
    color: var(--text-secondary);
  }

  .sort-chips {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .sort-chip {
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 14px;
    font-weight: 500;
    border: 2px solid var(--accent-amber);
    background: white;
    color: var(--text-secondary);
    cursor: pointer;
    transition: all 0.3s;
  }

  .sort-chip:hover {
    background: var(--accent-amber);
    color: var(--primary-dark);
  }

  .sort-chip.active {
    background: var(--accent-gold);
    color: var(--primary-dark);
    border-color: var(--accent-gold);
    font-weight: 700;
    box-shadow: 0 2px 8px var(--shadow-warm);
  }

  .clear-sort-btn {
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 500;
    border: none;
    background: #dc3545;
    color: white;
    cursor: pointer;
    transition: all 0.3s;
    margin-right: auto;
  }

  .clear-sort-btn:hover {
    background: #c82333;
  }

  @media (max-width: 768px) {
    .series-title {
      font-size: 32px;
    }

    .stats-grid {
      grid-template-columns: 1fr;
    }

    .series-grid {
      grid-template-columns: 1fr;
    }

    .lecture-number-badge {
      width: 40px;
      height: 40px;
      font-size: 18px;
    }

    .sort-controls {
      flex-direction: column;
      align-items: flex-start;
    }

    .sort-chips {
      width: 100%;
    }

    .clear-sort-btn {
      margin-right: 0;
      margin-top: 8px;
    }
  }
</style>

<div class="series-detail-page">
  <!-- Breadcrumb -->
  <nav class="breadcrumb">
    <div class="container">
      <a href="/"><%= locale === 'ar' ? 'الرئيسية' : 'Home' %></a>
      <span class="separator">›</span>
      <span class="current"><%= locale === 'ar' ? series.titleArabic : (series.titleEnglish || series.titleArabic) %></span>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="series-hero">
    <div class="container">
      <% if (series.category) { %>
        <div class="series-category-badge"><%= translateCategory(series.category) %></div>
      <% } %>

      <% if (locale === 'ar') { %>
        <h1 class="series-title"><%= series.titleArabic %></h1>
        <% if (series.titleEnglish) { %>
          <h2 class="series-title-en"><%= series.titleEnglish %></h2>
        <% } %>
      <% } else { %>
        <h1 class="series-title"><%= series.titleEnglish || series.titleArabic %></h1>
        <% if (series.titleEnglish && series.titleArabic !== series.titleEnglish) { %>
          <h2 class="series-title-en"><%= series.titleArabic %></h2>
        <% } %>
      <% } %>

      <!-- Sheikh Info -->
      <% if (series.sheikhId) { %>
        <div class="series-sheikh">
          <svg class="icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
          <a href="/sheikhs/<%= series.sheikhId.slug ? encodeURIComponent(series.sheikhId.slug) : series.sheikhId._id %>">
            <%= locale === 'ar' ? series.sheikhId.nameArabic : (series.sheikhId.nameEnglish || series.sheikhId.nameArabic) %>
            <% if (locale === 'ar' && series.sheikhId.honorific) { %>
              <span class="honorific"><%= series.sheikhId.honorific %></span>
            <% } %>
          </a>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Statistics -->
  <div class="stats-section">
    <div class="container">
      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <polygon points="10 8 16 12 10 16 10 8"></polygon>
            </svg>
          </div>
          <div class="stat-content">
            <div class="stat-value"><%= stats.totalLectures %></div>
            <div class="stat-label"><%= locale === 'ar' ? 'محاضرة' : 'Lectures' %></div>
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polygon points="23 7 16 12 23 17 23 7"></polygon>
              <polygon points="14 7 7 12 14 17 14 7"></polygon>
            </svg>
          </div>
          <div class="stat-content">
            <div class="stat-value"><%= locale === 'ar' ? stats.totalPlays.toLocaleString('ar-SA') : stats.totalPlays.toLocaleString('en-US') %></div>
            <div class="stat-label"><%= locale === 'ar' ? 'استماع' : 'Plays' %></div>
          </div>
        </div>

        <div class="stat-card">
          <div class="stat-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <polyline points="12 6 12 12 16 14"></polyline>
            </svg>
          </div>
          <div class="stat-content">
            <div class="stat-value"><%= Math.floor(stats.totalDuration / 3600) %></div>
            <div class="stat-label"><%= locale === 'ar' ? 'ساعة محتوى' : 'Hours' %></div>
          </div>
        </div>

        <% if (stats.completeLectures > 0) { %>
          <div class="stat-card">
            <div class="stat-icon">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22 4 12 14.01 9 11.01"></polyline>
              </svg>
            </div>
            <div class="stat-content">
              <div class="stat-value"><%= Math.round((stats.completeLectures / stats.totalLectures) * 100) %>%</div>
              <div class="stat-label"><%= locale === 'ar' ? 'مرقمة' : 'Numbered' %></div>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <!-- Description -->
  <% if (series.descriptionArabic || series.descriptionEnglish) { %>
    <div class="content-section">
      <div class="container">
        <h2 class="section-title"><%= locale === 'ar' ? 'عن السلسلة' : 'About the Series' %></h2>
        <div class="description-card">
          <% if (locale === 'ar') { %>
            <% if (series.descriptionArabic) { %>
              <p class="description-text"><%= series.descriptionArabic %></p>
            <% } %>
          <% } else { %>
            <% if (series.descriptionEnglish) { %>
              <p class="description-text"><%= series.descriptionEnglish %></p>
            <% } else if (series.descriptionArabic) { %>
              <!-- Fallback to Arabic if no English description -->
              <p class="description-text"><%= series.descriptionArabic %></p>
            <% } %>
          <% } %>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Related Khutba Series (for consolidated خطب الجمعة only) -->
  <% if (relatedKhutbaSeries && relatedKhutbaSeries.length > 0) { %>
    <div class="content-section">
      <div class="container">
        <h2 class="section-title"><%= locale === 'ar' ? 'سلاسل الخطب' : 'Khutbah Series' %></h2>
        <div class="series-grid">
          <% relatedKhutbaSeries.forEach(relSeries => { %>
            <a href="/series/<%= relSeries.slug ? encodeURIComponent(relSeries.slug) : relSeries._id %>" class="series-card">
              <div class="series-icon">
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
                  <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
                </svg>
              </div>
              <div class="series-content">
                <h3 class="series-title"><%= locale === 'ar' ? relSeries.titleArabic : (relSeries.titleEnglish || relSeries.titleArabic) %></h3>
                <% if (locale === 'en' && relSeries.titleEnglish && relSeries.titleArabic !== relSeries.titleEnglish) { %>
                  <p class="series-title-en"><%= relSeries.titleArabic %></p>
                <% } else if (locale === 'ar' && relSeries.titleEnglish && relSeries.titleEnglish !== relSeries.titleArabic) { %>
                  <p class="series-title-en"><%= relSeries.titleEnglish %></p>
                <% } %>
                <div class="series-meta">
                  <% if (relSeries.category) { %>
                    <span class="series-category"><%= translateCategory(relSeries.category) %></span>
                  <% } %>
                  <span class="series-count"><%= relSeries.actualLectureCount || 0 %> <%= locale === 'ar' ? 'محاضرة' : 'lectures' %></span>
                </div>
              </div>
            </a>
          <% }); %>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Lectures Section -->
  <div class="content-section">
    <div class="container">
      <h2 class="section-title"><%= relatedKhutbaSeries && relatedKhutbaSeries.length > 0 ? (locale === 'ar' ? 'خطب متفرقة' : 'Miscellaneous Khutbahs') : (locale === 'ar' ? 'محاضرات السلسلة' : 'Series Lectures') %> (<%= lectures.length %>)</h2>

      <% if (lectures && lectures.length > 0) { %>
        <!-- Sort Controls -->
        <div class="sort-controls">
          <span class="sort-label"><%= locale === 'ar' ? 'ترتيب:' : 'Sort:' %></span>
          <div class="sort-chips">
            <button class="sort-chip active" data-sort="order" onclick="sortLectures('order')">
              <%= locale === 'ar' ? 'الترتيب الأصلي' : 'Original Order' %>
            </button>
            <button class="sort-chip" data-sort="number" onclick="sortLectures('number')">
              <%= locale === 'ar' ? 'حسب الرقم' : 'By Number' %>
            </button>
            <button class="sort-chip" data-sort="oldest" onclick="sortLectures('oldest')">
              <%= locale === 'ar' ? 'الأقدم أولاً' : 'Oldest First' %>
            </button>
            <button class="sort-chip" data-sort="newest" onclick="sortLectures('newest')">
              <%= locale === 'ar' ? 'الأحدث أولاً' : 'Newest First' %>
            </button>
          </div>
          <button class="clear-sort-btn" onclick="clearSort()" id="clearSortBtn" style="display: none;">
            <%= locale === 'ar' ? 'مسح' : 'Clear' %> ✕
          </button>
        </div>

        <div class="lectures-list" id="lecturesList">
          <% lectures.forEach((lecture, index) => { %>
            <div class="lecture-item" data-sort-order="<%= lecture.sortOrder != null ? lecture.sortOrder : index %>" data-lecture-number="<%= lecture.lectureNumber || 999 %>" data-date="<%= lecture.dateRecorded ? new Date(lecture.dateRecorded).getTime() : 0 %>" data-index="<%= index %>">
              <% if (lecture.lectureNumber) { %>
                <div class="lecture-number-badge">
                  <%= lecture.lectureNumber %>
                </div>
              <% } else { %>
                <div class="lecture-number-badge unnumbered">
                  <%= index + 1 %>
                </div>
              <% } %>
              <div class="lecture-item-content">
                <%- include('../partials/lectureCard', { lecture }) %>
              </div>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="empty-state">
          <div class="empty-icon">📚</div>
          <h3><%= locale === 'ar' ? 'لا توجد محاضرات في هذه السلسلة حالياً' : 'No lectures in this series currently' %></h3>
        </div>
      <% } %>
    </div>
  </div>
</div>

<script>
  // Play audio using sticky player
  function playAudio(lectureId, title, sheikh) {
    console.log('▶️ Play from series detail page');

    if (window.audioPlayer) {
      window.audioPlayer.play(lectureId, {
        title: title,
        titleArabic: title,
        sheikh: sheikh || ''
      });
    } else {
      console.error('❌ Audio player not initialized');
      alert('مشغل الصوت يتم تحميله، يرجى المحاولة مرة أخرى');
    }
  }

  // Sort lectures
  let currentSort = 'order';

  function sortLectures(sortType) {
    const lecturesList = document.getElementById('lecturesList');
    if (!lecturesList) return;

    const lectures = Array.from(lecturesList.querySelectorAll('.lecture-item'));

    // Sort lectures based on type
    lectures.sort((a, b) => {
      if (sortType === 'order') {
        // Original order from Excel/import (uses sortOrder field)
        const orderA = parseInt(a.dataset.sortOrder) || 0;
        const orderB = parseInt(b.dataset.sortOrder) || 0;
        return orderA - orderB;
      } else if (sortType === 'number') {
        // By lecture number (extracted from title)
        const numA = parseInt(a.dataset.lectureNumber) || 999;
        const numB = parseInt(b.dataset.lectureNumber) || 999;
        return numA - numB;
      } else if (sortType === 'oldest') {
        const dateA = parseInt(a.dataset.date) || 0;
        const dateB = parseInt(b.dataset.date) || 0;
        // If dates are equal or both 0, fall back to sort order
        if (dateA === dateB) {
          const orderA = parseInt(a.dataset.sortOrder) || 0;
          const orderB = parseInt(b.dataset.sortOrder) || 0;
          return orderA - orderB;
        }
        return dateA - dateB;
      } else if (sortType === 'newest') {
        const dateA = parseInt(a.dataset.date) || 0;
        const dateB = parseInt(b.dataset.date) || 0;
        // If dates are equal or both 0, fall back to sort order (descending)
        if (dateA === dateB) {
          const orderA = parseInt(a.dataset.sortOrder) || 0;
          const orderB = parseInt(b.dataset.sortOrder) || 0;
          return orderB - orderA;
        }
        return dateB - dateA;
      }
      return 0;
    });

    // Re-append lectures in sorted order
    lectures.forEach(lecture => lecturesList.appendChild(lecture));

    // Update active chip
    document.querySelectorAll('.sort-chip').forEach(chip => {
      chip.classList.toggle('active', chip.dataset.sort === sortType);
    });

    // Show clear button if not default sort
    const clearBtn = document.getElementById('clearSortBtn');
    if (clearBtn) {
      clearBtn.style.display = sortType !== 'order' ? 'block' : 'none';
    }

    currentSort = sortType;
  }

  function clearSort() {
    sortLectures('order');
  }
</script>
